#include <stdio.h>

/**
 * main - prints name of file to be run.
 * @argc: Number of command line arguments.
 * @argv: Argument vector of pointers to strings
 * Return: 0.
 */
int main(int argc __attribute__((unused)), char *argv[])
{
int i = 0;

printf("%s\n", argv[i]);
return (0);
}
