#include "main.c"

/**
 * _strlen - function that returns the length of a string.
 * @l : l is a character
 * Return: value is m
 */
int _strlen(char *l)
{
int m;

for (m = 0; l[m] != '\0'; m++)
{

}
return (m);
}
