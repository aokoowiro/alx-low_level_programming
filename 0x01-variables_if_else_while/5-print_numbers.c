#include <stdio.h>

/**
 * main - entry point
 *
 * Description: print all single decimal numbers starting from 0
 *
 * Return: returns 0
 */

int main(void)
{
int f;

do {
printf("%d", f);
f++;
} while (f <= 9);
putchar('\n');
return (0);
}
